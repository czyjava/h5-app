# 当前任务

- 项目：`h5-magicpen`
- 目标：基于 APK 页面走查和登录态抓包结果，实现神笔绘画 H5 复刻原型。
- 范围：移动端 H5 页面、核心交互、抓包数据动态加载、API 适配分层。
- 非目标：不接入真实登录、不提交生成/支付/上传到生产服务端、不存储真实 token。
- 验收：
  - 可以本地启动并构建。
  - 首页、灵感、拍摄、涂色、我的五个主入口可切换。
  - 做同款、涂色画布、作品详情、设置等关键交互可打开。
  - 目录分层清晰，后续可替换真实 API。
- 当前状态：
  - 已完成实时接口优先请求、抓包 fixture 兜底和接口日志链路。
  - 已验证本地构建、首屏接口请求、实时接口失败回退和设置入口。
  - 2026-05-19 已用 Android 模拟器重启原 APP，并基于首页、灵感、涂色、拍摄、我的页截图继续复刻 H5 视觉。
  - 已完成 API 调试面板亮色 Network 布局、JSON 树响应体、首页/灵感/涂色/拍摄/我的页第一轮贴近原 APP 的样式调整。
  - 已修复社区帖子封面与头像映射，灵感页从实时接口显示真实封面和作者头像。
  - 已把“我的”页复刻展示和真实登录态解耦，未登录时也按模拟器登录态展示昵称、钻石和作品瀑布流。
  - 真实账号同步入口仍保留在“登录同步数据”和设置抽屉中，用户主动登录后会切回实时账号数据。
  - 已接入认证域 `current-user` 接口，H5 写入用户主动提供的临时登录态后可展示真实昵称、头像和作品列表；明文 token 未写入文件。
  - 2026-05-19 二轮对齐“我的”页：顶部资料区上移，VIP 卡压缩为原 APP 的宽扁样式，补齐作品视图切换按钮，移除作品标题遮罩和快捷入口滚动条。
  - 已关闭主界面的 API 调试浮球，仅保留 `#/api-debug` 调试页；拍摄页模式条滚动条已隐藏。
  - 涂色页补齐默认等级 `L1 新手画师`，分组标题改为 `入门·水果蔬菜 0/8完成`。
  - 已修复底部主导航被全局相对定位覆盖后掉出手机壳的问题，首页、我的、涂色页导航重新固定在底部，相机页继续隐藏导航。
  - 设置页已新增业务接口环境单选，勾选测试环境后业务域从 `pixel-studio.wanmeixiangsu.cn` 切到 `pixel-studio.ttt.wanmeixiangsu.cn`，登录和配置域不受影响。
  - 设置弹层打开时已提升内容层级，避免底部导航遮挡环境切换和设置项；token 输入框改为密码态显示。
  - 已修复短信登录缺少隐藏 `smsId` 导致后端提示“参数:smsId 不能为空”的问题，`smsId` 由发送验证码接口返回后自动带入登录请求，不暴露给用户填写。
  - “我的”页快捷入口已补齐交互：邀请有礼、教育优惠、演示模式、活动都会打开对应功能面板；演示模式可在当前会话内切换脱敏展示态。
  - 已按 APP 抓包重做网络层：公共 query 对齐 Android 画像，请求头由代理重建为 Android WebView + gzip，不再透传浏览器头。
  - `quote-by-template` 对齐抓包中的 POST form 语义：query 只保留公共签名参数，body 发送 `apiVersion`、`templateCode`、`templatePrice`，并从首页推荐 `jumpLink` 提取 `templateCode`；本地代理暂不启用 APP 私有 `tnpn2/tnpn4` 编码。
  - 已在 `/Users/chenzhiyuan/work/codes/wmxs/h5-app/common/src/network` 抽出公共网络 profile、公共 query、form 参数和上游请求头构造，供后续 APP 复刻复用。
  - 已补齐“钻石明细”页面：从原 APP 抓包确认接口为 `/api/open/user-token/bill.htm?page=1&limit=20&apiVersion=1`，H5 新增 `#/profile/diamond-detail`，展示余额卡、立即充值入口和钻石流水列表。
  - 已重新对照模拟器首页和灵感页截图：接入 APK 内首页 logo、顶部背景和底部导航图标；首页卡片背景改为抓包素材组中的大图/视频封面，标签缩略图改为对应小图；新增首页右下“海底世界”可关闭悬浮入口，灵感页不展示该浮层以对齐原 APP。
  - 2026-05-22 已参照 HomeAI 做法新增神笔绘画 APP 测试账号映射，固定使用分配手机号 `99900000089` 查询短信验证码，验证码只用于当次回填且不写入日志或文档。
  - 2026-05-22 已下载并安装神笔绘画 `5.26.0` 最新 APK，对首页、灵感、涂色、我的和绘画扫描入口重新做 APK-H5 差异对比；报告见 `ai/diff-runs/2026-05-22-latest-apk/report.md`。
  - 2026-05-22 已修复最新 APK 对比中明确的 H5 差异：绘画扫描未登录拦截、我的页未登录态、口令福利入口、涂色分组状态、首页会员 pill 和灵感页做同款次数显示，并新增 `npm run test:diff-fixes` 契约检查。
